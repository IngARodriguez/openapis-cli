// Single source of truth for what we configure.

export const BASE_URL = "https://api.openapis.online/anthropic";
export const DEFAULT_KEY = "adminn69";

// Used by `test` command — hits a route that exists and is cheap.
export const STATUS_URL = "https://api.openapis.online/status";
